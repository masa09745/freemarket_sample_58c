.list__card--logobtn
  .logo
    = render 'logo'
  .btn
    = form_tag(delete_cards_path, method: :post, id: 'charge-form') do
      = text_field_tag 'card_id', '', type: 'hidden'
      = submit_tag '削除する', id: 'token_delete', class: 'delete-btn'
.list__card--number
  = "************" + @card_info.last4
.list__card--date
  - exp_month = @card_info.exp_month.to_s
  - exp_year = @card_info.exp_year.to_s.slice(2,3)
  = exp_month + " / " + exp_year