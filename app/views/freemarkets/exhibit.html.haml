.content
  =render "shared/header"
  - if current_user.id == @freemarket.user_id
    %section.content-main
      %h1.goods-name
        = @freemarket.item
      %p.goods-info
        この商品は
        = @freemarket.user.nickname
        さんが出品した商品になります
      .goods-main
        .goods-photo
          .goods-photo__main
            = image_tag @freemarket.item_images[0].image_url.url, size:'300x300'
        %table.goods-data
          %tr
            %th 出品者
            %td 
              = @freemarket.user.nickname
          %tr
            %th カテゴリー
            %td テストカテゴリ
          %tr
            %th ブランド
            %td テストブランド
          %tr
            %th 商品サイズ
            %td M
          %tr
            %th 商品の状態
            %td 
              = @freemarket.condition
          %tr
            %th 配送料の負担
            %td 
              = @freemarket.ship_charge
          %tr
            %th 配送の方法
            %td フリマらくらく便
          %tr
            %th 配送元地域
            %td
              =@freemarket.ship_from
          %tr
            %th 発送日の目安
            %td 
              = @freemarket.ship_day
    .edit
      .edit-form
        %button{class:'form-btn edit-btn'}
          %span 商品編集する
        %p
          or
        %button{class:'form-btn gray-btn comment-send'}
          %span 出品を一時停止する
        = button_to 'この商品を削除する', freemarket_path(@freemarket.id), { method: :delete, id:'item-delete', class:'form-btn gray-btn', data: { confirm: '本当に削除しますか？'} }
    .message
      .message-box
      .message-form
        %form.message-form__content
          %p 相手のことを考え丁寧なコメントを心がけましょう。　不快な言葉遣いなどは利用制限や退会処分となることがあります。
          %textarea{type:'text', name:'message', class:'form-text'}
          %button{type:'submit', class:'form-btn gray-btn comment-send'}
            %span コメントする

  = render partial: 'shared/footertop'
  = render partial: 'shared/footer'
