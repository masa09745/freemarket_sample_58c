%header
  =render "/shared/header"
.category-main
  %h2 カテゴリー一覧
  .category-parent
    - @parents.each do |parent|
      =link_to "", class:"category-parent__list" do
        =parent.category_name
  .category-list-box
    .category-parent-title
      %h3 レディース
    .category-list-box__content
      - @ladys.each do |lady|
        .child
          .child-name
            =lady.category_name
          .grandchild
            - lady.children.each do |grandchild|
              =link_to category_path(grandchild), class:"grandchild__list" do
                =grandchild.category_name
  .category-list-box
    .category-parent-title
      %h3 メンズ
    .category-list-box__content
      - @mens.each do |men|
        .child
          .child-name
            =men.category_name
          .grandchild
            - men.children.each do |grandchild|
              =link_to"", class:"grandchild__list" do
                =grandchild.category_name
  .category-list-box
    .category-parent-title
      %h3 ベビー・キッズ
    .category-list-box__content
      - @kids.each do |kid|
        .child
          .child-name
            =kid.category_name
          .grandchild
            - kid.children.each do |grandchild|
              =link_to"", class:"grandchild__list" do
                =grandchild.category_name
  .category-list-box
    .category-parent-title
      %h3 インテリア・住まい・小物
    .category-list-box__content
      - @lifes.each do |life|
        .child
          .child-name
            =life.category_name
          .grandchild
            - life.children.each do |grandchild|
              =link_to"", class:"grandchild__list" do
                =grandchild.category_name
  .category-list-box
    .category-parent-title
      %h3 本・音楽・ゲーム
    .category-list-box__content
      - @books.each do |book|
        .child
          .child-name
            =book.category_name
          .grandchild
            - book.children.each do |grandchild|
              =link_to"", class:"grandchild__list" do
                =grandchild.category_name
  .category-list-box
    .category-parent-title
      %h3 おもちゃ・ホビー・グッズ
    .category-list-box__content
      - @hobbies.each do |hobby|
        .child
          .child-name
            =hobby.category_name
          .grandchild
            - hobby.children.each do |grandchild|
              =link_to"", class:"grandchild__list" do
                =grandchild.category_name
.footertop
  =render "/shared/footertop"
.footer
  =render "/shared/footer"
